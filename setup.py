from setuptools import setup

setup(name='declarative_elements',
      version='0.1',
      description='Package to define relationship between selenium web elements in OOP style',
      url='http://github.com/TODO',
      packages=['declarative_elements'],
      python_requires='>=3.7',
      install_requires=[
          'selenium',
      ])
